import React from "react";
import './Header.css'

const Header = () => {
    return (
        <header className="header">
            <img src="Recipes-img/header-logo.png" alt="header-logo"/>
        </header>
    );
};

export default Header;